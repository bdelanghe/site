# Outreach blurbs

Reusable copy for LinkedIn notes, cold emails, and cover notes. Customize the
`[bracket]`. Voice drawn from the same contract as the site (`data/profile.json`):
the security layer for agentic systems, signed effects, contracts as build failures.

## One-line hook
Senior SWE building the **security layer for agentic systems** — capability-based
security where every privileged effect is verified against a signed owner. Building
`prx` + bounded-systems in the open.

## Universal
I'm a senior software engineer working on the **security layer for agentic systems** —
capability-based security where every privileged effect an agent performs is verified
against a signed owner, and contracts are enforced as build failures rather than caught
in review. At Aura I architected the contract-and-validation layer behind our
inventory-purchasing decisions: one source of truth, CI drift-gated across 130+
components, delivered as agent-authored PRs. I build `prx` and the bounded-systems
capability libraries in the open. I'd love to talk if you're working on `[X]`.

## Agent infrastructure
*(Credal, Sierra, Factory, AgentMail, Thread AI, Scaled Cognition…)*

You're building infrastructure for agents; I build the part that keeps them **bounded** —
`guest-room` (a rooms-and-doors capability runtime) and `prx` (capability-scoped agents
whose every privileged effect is verified against a signed owner, driven through one
signed pipeline to a merged PR). I've shipped agent-authored delivery in production at
Aura. The substrate that lets agents act safely is exactly my focus — I'd love to
compare notes on `[your agent platform]`.

## AI security / safety / provenance
*(Haize, Aloe Identity, Palace, FAR AI, Outtake…)*

My work is capability-based security for AI agents: **every privileged effect
attributable to a signed owner**, provenance and contract checks gating every output
(`ocap-provenance` maps this to SLSA). At Aura I made a 130+-component pipeline auditable
by construction, with sign-off bound to an owner-role enforced in CI. If you're working
on making agent systems trustworthy at the systems level, that's the problem I've been
living in.

## Developer tools / platform
*(Git AI, East River Source Control, Convex, Graphite, Modal…)*

I build developer platforms with rigor baked in: a contract-and-validation layer at Aura
(one source of truth → schemas/docs/tests/agent-context, drift-gated in CI), and before
that a design system that cut task lead-time ~30%, static-analysis PR gating that cut
critical bugs ~20%, and a perf rebuild (~95% faster loads). My side work (`git-ast`,
deterministic Nix builds, typed contracts) is the same instinct. I'd love to help
`[your platform]` ship faster without losing correctness.
