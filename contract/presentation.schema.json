{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://robertdelanghe.dev/contract/presentation.schema.json",
  "title": "presentation / render-context contract",
  "description": "Homepage render context — banner, intro, seeking, the decorative place line, and the homepage nav links. Decorates the canonical JSON Resume doc (contract/jsonresume.schema.json) and never redefines its fields; build.mjs shallow-merges presentation over profile. The homepage proof line derives from the canonical projects[], not from here. Held to the same string-audit lens as the canonical copy.",
  "type": "object",
  "required": ["links"],
  "additionalProperties": false,
  "properties": {
    "_source": { "type": "string" },
    "banner": {
      "type": "object",
      "description": "Profile banner content tokens — the strings the generated header SVG renders (banner.mjs). Colors come from brand design tokens, not here.",
      "additionalProperties": false,
      "properties": {
        "tagline": { "type": "string", "description": "banner subtitle — one line under the name" },
        "stack": { "type": "array", "items": { "type": "string" }, "description": "small-caps tech list rendered under the tagline" }
      }
    },
    "intro": { "type": "string", "description": "hero — short first-person bio line, sits between headline and summary" },
    "place": { "type": "string", "description": "decorative hero line — affiliations (e.g. 'Brooklyn · Aura · Recurse Center · Bennington'). The résumé location comes from basics.location, not here." },
    "seeking": {
      "type": "object",
      "description": "Job-search callout: what roles you're open to.",
      "required": ["focus"],
      "additionalProperties": false,
      "properties": {
        "label": { "type": "string" },
        "focus": { "type": "string" },
        "detail": { "type": "string" },
        "cta": { "type": "string" },
        "href": { "type": "string" }
      }
    },
    "links": {
      "type": "array",
      "description": "Homepage nav links (and llms.txt). The résumé contact line uses basics.profiles + the canonical email, not these.",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["label", "href"],
        "additionalProperties": false,
        "properties": {
          "label": { "type": "string" },
          "href": { "type": "string" }
        }
      }
    }
  }
}
