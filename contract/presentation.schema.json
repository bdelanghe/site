{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://robertdelanghe.dev/contract/presentation.schema.json",
  "title": "presentation / render-context contract",
  "description": "Homepage render context — the deck, the availability rows, the authored case studies, and the homepage nav links. Decorates the canonical JSON Resume doc (contract/jsonresume.schema.json) and never redefines its fields; build.mjs shallow-merges presentation over profile. Held to the same string-audit lens as the canonical copy. The homepage opens with ONE positioning statement: basics.headline is the statement, `deck` is its single supporting line, and basics.summary is résumé copy that the homepage deliberately does not repeat.",
  "type": "object",
  "required": ["links"],
  "additionalProperties": false,
  "properties": {
    "_source": { "type": "string" },
    "banner": {
      "type": "object",
      "description": "Profile banner content tokens — the strings the generated header SVG renders (banner.mjs). Colors come from brand design tokens, not here.",
      "additionalProperties": false,
      "properties": {
        "tagline": { "type": "string", "description": "banner subtitle — one line under the name" },
        "stack": { "type": "array", "items": { "type": "string" }, "description": "small-caps tech list rendered under the tagline" }
      }
    },
    "deck": { "type": "string", "description": "hero — the ONE line under the headline. Names the fields the selected work covers; it does not restate the headline's thesis." },
    "origin": { "type": "string", "description": "one sentence in the record margin tying the architecture / sculpture training to the systems work. Personal texture, not positioning — keep it to a single sentence." },
    "now": {
      "type": "object",
      "description": "Where the work happens, as ruled label/value rows — NOT an availability pitch. It replaced `seeking` when the job search ended: a block that advertises openness to roles nobody is open to is a dead call to action taking up the best position on the page. Deliberately two rows and one note: everything the r\u00e9sum\u00e9 record already states (title, dates, history) is NOT repeated here, and the only outbound action offered is the one a reader can actually take.",
      "required": ["role"],
      "additionalProperties": false,
      "properties": {
        "role": { "type": "string", "description": "the CURRENTLY row's value \u2014 employer and place. Not the title: the masthead carries basics.label and the record carries the full entry." },
        "roleHref": { "type": "string", "description": "employer link" },
        "note": { "type": "string", "description": "OPTIONAL one short line under the rows. Currently unset, and that is the default state, not an omission: it briefly advertised the employer's open roles, which was wrong once the posting in question was the one this r\u00e9sum\u00e9's owner filled. Set it only for something a reader can act on that is TRUE at the time of writing; a stale pointer is worse than none." },
        "noteHref": { "type": "string", "description": "where the note points. Required in practice whenever `note` is set \u2014 an un-actionable call to action is not one." }
      }
    },
    "caseStudies": {
      "type": "array",
      "description": "The homepage's authored dossier entries — the unit of professional value, replacing the repository card as the thing the page argues with. Each is numbered in render order and answers the same four questions, so the set reads as one instrument: what was wrong, what was done, what shows it worked, and which parts were the author's. An editorial set of three to five; a repository is evidence inside an entry, never an entry on its own. Every string here is audited exactly like the résumé copy, so a figure must be in data/audit/grounding.json.",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["name", "kicker", "problem", "intervention", "evidence", "role"],
        "additionalProperties": false,
        "properties": {
          "name": { "type": "string", "description": "the system's name — an org, a product surface, or a program of work" },
          "kicker": { "type": "string", "description": "one line under the name — what the system is" },
          "problem": { "type": "string", "description": "what was wrong, stated as a condition rather than a task" },
          "intervention": { "type": "string", "description": "what was changed, in the author's own terms" },
          "evidence": { "type": "string", "description": "what shows it worked — and, where the record is incomplete, what is still open" },
          "role": { "type": "string", "description": "which parts of the work were the author's" },
          "links": {
            "type": "array",
            "description": "public artefacts backing the entry. Omitted where the work is not public — an entry without links is not a weaker entry.",
            "items": {
              "type": "object",
              "required": ["label", "href"],
              "additionalProperties": false,
              "properties": {
                "label": { "type": "string" },
                "href": { "type": "string" }
              }
            }
          }
        }
      }
    },
    "links": {
      "type": "array",
      "description": "Homepage nav links (and llms.txt). Email is NOT one of them — it is the availability block's CONTACT row, sourced from basics.email, so the address renders once. The résumé contact line uses basics.profiles + the canonical email, not these.",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["label", "href"],
        "additionalProperties": false,
        "properties": {
          "label": { "type": "string" },
          "href": { "type": "string" }
        }
      }
    },
    "colophon": {
      "type": "array",
      "description": "\"Built with\" — the upstream tools that produce and validate this site (design system, gates, authoring vault, dev container), surfaced as a homepage colophon and linked to their repos. Each must be a real build input or gate; private repos are omitted (no dead links).",
      "items": {
        "type": "object",
        "required": ["name", "href"],
        "additionalProperties": false,
        "properties": {
          "name": { "type": "string", "description": "tool / repo name" },
          "role": { "type": "string", "description": "one line — what it does in the build" },
          "href": { "type": "string", "description": "upstream repo / package URL (public)" }
        }
      }
    }
  }
}
