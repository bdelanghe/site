[
  {
    "symbol": "exp.aura.what",
    "check": "grounding",
    "note": "Grounded by sourced evidence kept in the private worklog (merged-PR history + commerce projects led). No inline metric on the public site, by choice; receipts stay private."
  },
  {
    "symbol": "exp.aura.b2",
    "check": "grounding",
    "note": "Each named automation is a sourced journal line in the private worklog: ocean inbound into DCL (3PL), US Amazon LTL breakdown+automation, and the 3PL/Amazon freight splits — all Notion Commerce Projects (Project Lead). Qualitative by choice; receipts stay private."
  },
  {
    "symbol": "exp.bounded-systems.b0",
    "check": "grounding",
    "note": "58 = the public repository count of the bounded-systems org (github.com/orgs/bounded-systems/repositories) — publicly verifiable. Descriptive scale, not a performance metric."
  },
  {
    "symbol": "copy.llms.machine.api",
    "check": "grounding",
    "note": "Typed `claim` only because claimOrBody() routes any string containing a digit through the grounding registry, and this one contains the path version `/api/v1`. A route segment is an identifier, not a metric — there is no measurable assertion here to ground. The heuristic is right to be blunt; this is the false positive it buys."
  },
  {
    "symbol": "copy.llms.machine.mcp",
    "check": "grounding",
    "note": "Same false positive: the digits are `sha-256` (a hash algorithm name) and `/site.sha256` (a filename). Neither is a measurable claim. The sentence's only assertion is a NEGATIVE one — that this surface does not check the digest against the manifest — which is the opposite of an overclaim."
  }
]
