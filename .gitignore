dist/
node_modules/
# Lighthouse CI writes its reports + assertion-results here on every `lhci autorun`,
# in CI and locally alike (13MB a run). A build artifact, like the two above.
.lighthouseci/
# Nix build materializes the flake-pinned brand source here (see flake.nix); it's a
# build artifact, not site content — same as node_modules/, never committed.
brand/
.wrangler/
result
result-*
.DS_Store
copy-review.md

prose-corpus.json
doc-corpus.json
