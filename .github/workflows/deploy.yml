name: deploy

# Two-phase, promote-the-artifact pipeline:
#   build   — hermetic nix build → keyless-sign the in-toto/SLSA statement and
#             the whole-site manifest (Sigstore/Rekor, no held key) → publish a
#             SIGNED, fully-deployable OCI artifact to GHCR.
#   deploy  — pull THAT artifact, cosign-verify it, and wrangler-deploy exactly
#             those bytes. Prod serves the signed artifact by construction.
#             Fail-closed: a signing/verify failure blocks the deploy, and the
#             edge check asserts the provenance reached prod. Disconnect
#             Cloudflare's Workers Builds Git App so the two don't double-deploy.
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write # OIDC: lets cosign mint a keyless Fulcio cert for build provenance
  packages: write # push the built site to GHCR as a signed OCI artifact

concurrency:
  group: deploy
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      oci: ${{ steps.publish.outputs.oci }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - uses: DeterminateSystems/nix-installer-action@90bb610b90bf290cad97484ba341453bd1cbefea # v19
      - name: Build (nix, reproducible)
        run: nix build .#site -L
      - name: Stage dist
        run: cp -rL result dist && chmod -R u+w dist

      # SPDX 2.3 SBOM over the whole supply chain (npm + Nix + brand), then the
      # in-toto/SLSA statement over the hermetic subjects — both keyless-signed
      # (cosign sign-blob → Fulcio cert from the Actions OIDC identity → Rekor). No
      # stored key. gen-sbom runs FIRST so the SBOM is an attestation subject;
      # check-sbom is the fail-closed completeness gate (SBOM ↔ attestation). BEFORE
      # resume.pdf so the statement's subjects stay reproducible. The SBOM blob is
      # signed in the OCI step (after the manifest), so its sidecar stays out of the
      # whole-site manifest like the other provenance sidecars.
      - name: Sign provenance statement + SBOM (keyless Sigstore)
        run: |
          nix develop .#deploy --command bash -euo pipefail -c '
            # Static JSON API + OpenAPI and the did:web + résumé Verifiable Credential,
            # emitted BEFORE the manifest so they are covered by site.sha256 like the
            # rest of dist/ (the VC blob is signed after the manifest, below).
            node scripts/gen-api.mjs
            node scripts/gen-identity.mjs
            node scripts/gen-sbom.mjs
            node scripts/gen-attestation.mjs
            node scripts/check-sbom.mjs
            cosign sign-blob --yes \
              --bundle dist/attestation.intoto.json.sigstore.json \
              dist/attestation.intoto.json
          '
        env:
          GITHUB_SHA: ${{ github.sha }}

      - name: Render static resume PDF (headless Chrome)
        # Rendered from the built page AFTER the in-toto statement: Chrome output
        # is non-deterministic + impure, so it stays out of the hermetic nix build
        # and that statement. It IS covered by the whole-site manifest below (the
        # honest record of the served bytes). Chrome is preinstalled on the runner;
        # a tiny local server lets absolute /brand asset paths resolve.
        run: |
          python3 -m http.server 8099 --directory dist &
          srv=$!
          for i in $(seq 1 20); do curl -fsS http://127.0.0.1:8099/resume.html >/dev/null 2>&1 && break; sleep 0.5; done
          google-chrome --headless --no-sandbox --disable-gpu --hide-scrollbars \
            --no-pdf-header-footer --virtual-time-budget=12000 \
            --print-to-pdf=dist/resume.pdf http://127.0.0.1:8099/resume.html
          kill "$srv" 2>/dev/null || true
          ls -la dist/resume.pdf

      # Content-address the whole served site (resume.pdf included), keyless-sign
      # that manifest, write /provenance.json, then publish the WHOLE dist/ (site
      # + provenance sidecars) to GHCR as one signed, deployable OCI artifact.
      - name: Sign + publish the signed OCI artifact (keyless Sigstore → GHCR)
        id: publish
        env:
          GHCR_REPO: ghcr.io/bdelanghe/robertdelanghe-dev
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REF: ${{ github.ref }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          GITHUB_WORKFLOW_REF: ${{ github.workflow_ref }}
        run: |
          nix develop .#deploy --command bash -euo pipefail -c '
            short=$(printf "%s" "$GITHUB_SHA" | cut -c1-7)
            node scripts/gen-sitemanifest.mjs
            cosign sign-blob --yes --bundle dist/site.sha256.sigstore.json dist/site.sha256
            # Keyless-sign the SBOM blob (same Fulcio/Rekor identity). AFTER the
            # manifest: dist/sbom.spdx.json is covered by site.sha256, while its
            # signature sidecar stays out of the manifest, matching the attestation +
            # manifest sidecars. Both ride inside the signed OCI artifact (it is in dist/).
            cosign sign-blob --yes --bundle dist/sbom.spdx.json.sigstore.json dist/sbom.spdx.json
            # Keyless-sign the résumé Verifiable Credential the same way (enveloping
            # Sigstore proof). AFTER the manifest, like the SBOM: resume.vc.json is
            # covered by site.sha256, its signature sidecar stays out of the manifest.
            cosign sign-blob --yes --bundle dist/api/v1/resume.vc.json.sigstore.json dist/api/v1/resume.vc.json
            OCI_REF="$GHCR_REPO:$short" node scripts/gen-provenance.mjs
            # Content-address the served site as an IPFS UnixFS dir CID (no daemon) and
            # record it in provenance.json alongside the manifest digest — AFTER
            # gen-provenance writes that file. Uses the signed site.sha256 file set.
            node vendor/conformance-kit/generators/gen-cid.mjs
            tar --sort=name --owner=0 --group=0 --numeric-owner --mtime=@0 -C dist -cf site.tar .
            gzip -n -f site.tar
            printf "%s" "$GITHUB_TOKEN" | oras login ghcr.io -u "$GITHUB_ACTOR" --password-stdin
            oras push "$GHCR_REPO:$short" \
              --artifact-type application/vnd.robertdelanghe.site.v1+gzip \
              site.tar.gz:application/gzip | tee oras-push.log
            oras tag "$GHCR_REPO:$short" latest
            digest=$(awk "/Digest:/ {print \$2}" oras-push.log)
            printf "%s" "$GITHUB_TOKEN" | cosign login ghcr.io -u "$GITHUB_ACTOR" --password-stdin
            cosign sign --yes "$GHCR_REPO@$digest"
            echo "oci=$GHCR_REPO@$digest" >> "$GITHUB_OUTPUT"
          '

  # PREVIEW: upload the signed bytes as a NEW Worker version — Cloudflare gives it a unique
  # preview URL, but it is NOT served to production. This is the artifact you actually review.
  # NOTE: for this gate to mean anything, Cloudflare's Workers Builds Git App must be
  # DISCONNECTED (otherwise it auto-deploys every push to main, bypassing the approval).
  preview:
    needs: build
    runs-on: ubuntu-latest
    if: needs.build.outputs.oci != ''
    outputs:
      version_id: ${{ steps.upload.outputs.version_id }}
      preview_url: ${{ steps.upload.outputs.preview_url }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: DeterminateSystems/nix-installer-action@90bb610b90bf290cad97484ba341453bd1cbefea # v19

      # Pull the SIGNED artifact, verify its keyless signature against this repo's identity,
      # unpack exactly those bytes. Verify failure → no upload.
      - name: Pull + verify the signed OCI artifact
        env:
          OCI: ${{ needs.build.outputs.oci }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          nix develop .#deploy --command bash -euo pipefail -c '
            printf "%s" "$GITHUB_TOKEN" | cosign login ghcr.io -u "$GITHUB_ACTOR" --password-stdin
            cosign verify "$OCI" \
              --certificate-identity-regexp "^https://github.com/bdelanghe/site/" \
              --certificate-oidc-issuer https://token.actions.githubusercontent.com > /dev/null
            printf "%s" "$GITHUB_TOKEN" | oras login ghcr.io -u "$GITHUB_ACTOR" --password-stdin
            rm -rf oci-out dist && mkdir -p oci-out dist
            oras pull "$OCI" -o oci-out
            tar -xzf oci-out/site.tar.gz -C dist
          '

      # Upload a version (preview only — does not change what production serves). Capture the
      # version id (promoted later) + the preview URL (what you review before approving).
      # VERIFY: confirm `wrangler versions upload` output format + that it supports this
      # static-assets Worker, and the parsing below, on the first run.
      # Run wrangler directly via nix (no inner bash -c), capture output, then parse in the OUTER
      # shell where quoting is sane. NO_COLOR + an ANSI strip make the field extraction robust
      # (wrangler colorizes the version id; the raw log carries escape codes the GH UI hides).
      - name: Upload preview version
        id: upload
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          GITHUB_SHA: ${{ github.sha }}
          NO_COLOR: "1"
          FORCE_COLOR: "0"
        run: |
          short=$(printf '%s' "$GITHUB_SHA" | cut -c1-7)
          nix develop .#deploy --command wrangler versions upload --tag "sha-$short" --message "preview $short" 2>&1 | tee upload.log
          strip='s/\x1b\[[0-9;]*[mGKH]//g'
          vid=$(grep -F 'Worker Version ID:' upload.log | sed -E "$strip" | tail -1 | awk '{print $NF}')
          url=$(grep -F 'Version Preview URL:' upload.log | sed -E "$strip" | tail -1 | awk '{print $NF}')
          [ -n "$vid" ] || { echo "::error::could not parse Worker Version ID from upload output"; exit 1; }
          echo "version_id=$vid" >> "$GITHUB_OUTPUT"
          echo "preview_url=$url" >> "$GITHUB_OUTPUT"

      - name: Preview summary
        run: |
          {
            echo "### 🔍 Site preview ready — review before promoting"
            echo ""
            echo "Open the preview, then **approve the \`promote\` job** to ship it to robertdelanghe.dev."
            echo ""
            echo "- **Preview:** ${{ steps.upload.outputs.preview_url }}"
            echo "- **Version:** \`${{ steps.upload.outputs.version_id }}\`"
          } >> "$GITHUB_STEP_SUMMARY"

  # PROMOTE: gated behind a required-reviewers Environment. You review the preview above, then
  # approve — only then does this run, deploying the SAME reviewed version to 100% production.
  # The signed-provenance chain is intact because we promote the exact version that was previewed
  # (and re-verify the artifact before promoting).
  promote:
    needs: [build, preview]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && needs.preview.outputs.version_id != ''
    environment:
      name: site-promote
      url: https://robertdelanghe.dev
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: DeterminateSystems/nix-installer-action@90bb610b90bf290cad97484ba341453bd1cbefea # v19

      # Re-establish trust at promote time: pull + cosign-verify the signed artifact again, unpack
      # (needed by the prod-provenance check below).
      - name: Pull + verify the signed OCI artifact
        env:
          OCI: ${{ needs.build.outputs.oci }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          nix develop .#deploy --command bash -euo pipefail -c '
            printf "%s" "$GITHUB_TOKEN" | cosign login ghcr.io -u "$GITHUB_ACTOR" --password-stdin
            cosign verify "$OCI" \
              --certificate-identity-regexp "^https://github.com/bdelanghe/site/" \
              --certificate-oidc-issuer https://token.actions.githubusercontent.com > /dev/null
            printf "%s" "$GITHUB_TOKEN" | oras login ghcr.io -u "$GITHUB_ACTOR" --password-stdin
            rm -rf oci-out dist && mkdir -p oci-out dist
            oras pull "$OCI" -o oci-out
            tar -xzf oci-out/site.tar.gz -C dist
          '

      # Promote the exact reviewed version to 100% production. A single version spec with no
      # percentage gets all traffic ("omitted percentages share the remaining traffic", per
      # `wrangler versions deploy --help`) — avoids @100 vs @100% ambiguity.
      - name: Promote reviewed version to production
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          VERSION_ID: ${{ needs.preview.outputs.version_id }}
          NO_COLOR: "1"
        run: nix develop .#deploy --command wrangler versions deploy "$VERSION_ID" --yes

      # Assert the provenance reached prod (byte-identical manifest + provenance.json).
      - name: Verify prod serves the signed provenance
        run: |
          expected=$(sha256sum dist/site.sha256 | cut -d' ' -f1)
          echo "expected site.sha256=$expected"
          for i in 1 2 3 4 5; do
            live=$(curl -fsS https://robertdelanghe.dev/site.sha256 | sha256sum | cut -d' ' -f1) || live="(fetch failed)"
            code=$(curl -fsS -o /dev/null -w '%{http_code}' https://robertdelanghe.dev/provenance.json) || code="000"
            if [ "$live" = "$expected" ] && [ "$code" = "200" ]; then
              echo "prod serves the freshly-signed whole-site manifest + provenance.json ✓"
              exit 0
            fi
            echo "edge not fresh yet (attempt $i/5): manifest=$live provenance=$code"
            sleep 10
          done
          echo "::error::prod is not serving the freshly-signed provenance (manifest or provenance.json)"
          exit 1

      # Independent, cryptographic confirmation that prod serves verified bytes.
      # Retry: the signed bundle (site.sha256.sigstore.json) can reach the edge a few seconds after
      # the manifest, so a verify right after promote may check a fresh manifest against a stale
      # bundle ("signature verification failed"). Retry until the edge is consistent.
      - name: Cryptographically verify prod (standalone verifier)
        run: |
          npm ci --prefix vendor/conformance-kit/integrity/verify
          for i in 1 2 3 4 5 6; do
            if node vendor/conformance-kit/integrity/verify/verify.mjs https://robertdelanghe.dev; then exit 0; fi
            echo "verifier not consistent yet (attempt $i/6) — edge may still be propagating the signed bundle; retrying in 12s"
            sleep 12
          done
          echo "::error::standalone verifier failed after retries"
          exit 1

      # RFC 9110 HTTP-correctness probe of the live edge.
      - name: HTTP correctness probe (RFC 9110)
        run: PROBE_CONFIG=contract/http-probe.json node vendor/conformance-kit/integrity/http-probe.mjs https://robertdelanghe.dev
